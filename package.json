{
  "name": "dreyes-cms",
  "version": "1.0.0",
  "description": "CMS para D los Reyes - Carpintería & Diseño",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "seed": "node db/schema.js",
    "build:admin": "cd admin && npm run build",
    "setup": "npm install && cd admin && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^8.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
